h4 Ready to share yours?

button(id="start", class="btn btn-default btn-large") Start >

div.hr-divider
    div.hr-line
    span.hr-text or

h4#read Read some Testimonies

select#tag-category.dropdown
    option(value='0') Select a tag/category
    each tag in tags
        option(value=tag)= tag
